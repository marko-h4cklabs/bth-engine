# Anthropic (for AI Auditor step)
ANTHROPIC_API_KEY=

# Google Places API
GOOGLE_PLACES_API_KEY=

# Agency details (appear in PDF and landing page)
AGENCY_NAME=
AGENCY_DOMAIN=https://agencija.hr

# Netlify (landing page deploys — REST API, no CLI needed)
# Get token: Netlify dashboard → User Settings → Applications → Personal Access Tokens
NETLIFY_TOKEN=

# Notifications (Telegram)
TELEGRAM_BOT_TOKEN=   # From @BotFather
TELEGRAM_CHAT_ID=     # Your chat/channel ID

# Tracker server (bridges landing page visits → Telegram; run with: pnpm tracker)
TRACKER_PORT=3456
TRACKER_URL=http://localhost:3456   # Public URL if server is exposed (ngrok, VPS, etc.)

# Dashboard (local UI at http://localhost:4000)
DASHBOARD_PORT=4000

# Output paths
OUTPUT_DIR=./output
